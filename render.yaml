services:
  - type: web
    name: vocab-bot
    runtime: python
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python vocab_bot.py
    build:
      pythonVersion: 3.10
