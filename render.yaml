services:
  - type: web
    name: vocab-bot
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python vocab_bot.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.8
